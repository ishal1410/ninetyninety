__pycache__/
*.pyc
.env
.cache/
data/
out/
venv/
.venv/
.venv_diag/
.superpowers/
.bug-hunter/
.impeccable/review/
.impeccable/build/

# Strix pentest artifacts
strix_runs/

# GSD debug/planning sessions (local only)
.planning/

# Demo video: the sources are tracked, the renders and screen captures are not.
# Rebuild: python video/capture.py && python video/tts.py &&
#          python video/subs.py && python video/build.py
# capture.py films the hosted app, so it needs https://ninetyninety.streamlit.app
# awake. Without it there are no clips/ and build.py has nothing to render.
video/build/
video/clips/
video/audio/
video/audio.v1/
video/*.mp4
video/*.v1.py
video/*.v1.json
video/narration.v1/
