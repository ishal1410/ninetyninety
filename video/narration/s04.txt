Twelve rows at a time go through one Strands Agents graph. The Preparer and the Reviewer are two entry nodes, running in parallel. Strands hands each only the rows, so the Reviewer never sees the Preparer's reasoning. Both call a real Strands tool that returns the IRS instruction text for a line. Here is the finished trace: five graph runs, ninety two tool calls, counted by a Strands hook provider.
